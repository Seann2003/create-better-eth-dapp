// The Graph subgraph feature generator
import path from 'path';
import fs from 'fs-extra';
import { execa } from 'execa';

export async function generateSubgraph(projectDir: string) {
  console.log('Subgraph setup complete!');
}
